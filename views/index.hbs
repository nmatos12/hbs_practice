<!-- Output userName into an h1 -->
{{!-- <h1>{{userName}}</h1> --}}

<form action="/courses" method="POST">
    <input type="text" placeholder="Type your course name">
    <button>Submit</button>
</form>

<ul class="courses">
    {{#each courses}}
        <li>{{this.title}}</li>
    {{/each}}
</ul>

{{!-- 
<ul>
    {{#each fruits}}
        <li>{{this}}</li>
    {{/each}}
</ul>

{{#if loggedIn}}
    <p>User is logged in</p>
    {{else}}
    <p>User is not logged in</p>
{{/if}} --}}